# Insect Agent Atlas

Agent-native ecological architecture knowledge base lives at `insect-agent-atlas/`.

Agent entrypoint: `insect-agent-atlas/AGENTS.md`.
Machine manifest: `insect-agent-atlas/MANIFEST.json`.
